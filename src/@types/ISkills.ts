interface SkillInfo {
    color: string;
    icon: string;
}

export default interface ISkills {
    Java: SkillInfo;
    JavaScript: SkillInfo;
    TypeScript: SkillInfo;
    HTML: SkillInfo;
    CSS: SkillInfo;
    SCSS: SkillInfo;
    Dockerfile: SkillInfo;
    PHP: SkillInfo;
}